services:
  app:
    build: .
    container_name: what-todo-app
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
    networks:
      - app-network

networks:
  app-network:
    driver: bridge